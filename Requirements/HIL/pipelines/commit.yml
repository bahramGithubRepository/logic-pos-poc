Runnerinfo:
  stage: Runnerinfo
  tags:
    - HIL_Demo_Computer
  script:
      - Write-Host 'Connection established successfully!'
      - Write-Host 'Current working directory:' (Get-Location)
      - Set-Location "$CI_PROJECT_DIR/4_Automation/ConnectionToHil"
      - Write-Host 'Current working directory:' (Get-Location)
      - Write-Host 'PATH:' $env:Path
      - $env:Path = "C:\Users\TA_HILSolutions\.pyenv\pyenv-win\versions\3.8.10;C:\Users\TA_HILSolutions\.pyenv\pyenv-win\versions\3.8.10\Scripts;" + $env:Path
      
      - python --version
      - pip --version
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      when: always